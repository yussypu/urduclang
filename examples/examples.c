// === hello_urdu.c ===
// سب سے پہلا پروگرام: Hello World in Urdu C

#شامل <stdio.h>

عدد اصل() {
    چھاپو("!السلام علیکم، دنیا\n");
    واپس 0;
}


// === fibonacci_urdu.c ===
// فبوناچی سلسلہ: Fibonacci sequence in Urdu C

#شامل <stdio.h>

عدد فبوناچی(عدد ن) {
    اگر (ن <= 1) {
        واپس ن;
    }
    واپس فبوناچی(ن - 1) + فبوناچی(ن - 2);
}

عدد اصل() {
    کیلئے (عدد ش = 0; ش < 10; ش++) {
        چھاپو("%d ", فبوناچی(ش));
    }
    چھاپو("\n");
    واپس 0;
}


// === structs_urdu.c ===
// ساخت کا استعمال: Struct usage in Urdu C

#شامل <stdio.h>
#شامل <string.h>

ساخت طالبعلم {
    حرف نام[50];
    عدد عمر;
    اعشاری نمبر;
};

عدد اصل() {
    ساخت طالبعلم ط;
    strcpy(ط.نام, "یحییٰ");
    ط.عمر = 21;
    ط.نمبر = 9.5;

    چھاپو("نام: %s\n", ط.نام);
    چھاپو("عمر: %d\n", ط.عمر);
    چھاپو("نمبر: %.1f\n", ط.نمبر);

    واپس 0;
}
