// ساخت کا استعمال: Struct usage in Urdu C
// یہ پروگرام ساختوں کا استعمال دکھاتا ہے

#شامل <stdio.h>
#شامل <string.h>

// طالب علم کی ساخت
ساخت طالبعلم {
    حرف نام[50];
    عدد عمر;
    اعشاری نمبر;
};

// نقطہ کی ساخت
ساخت نقطہ {
    عدد ایکس;
    عدد وائی;
};

// فاصلہ حساب کرو
اعشاری فاصلہ(ساخت نقطہ ا, ساخت نقطہ ب) {
    عدد ڈ_ایکس = ب.ایکس - ا.ایکس;
    عدد ڈ_وائی = ب.وائی - ا.وائی;
    واپس (اعشاری)(ڈ_ایکس * ڈ_ایکس + ڈ_وائی * ڈ_وائی);
}

عدد اصل() {
    // طالب علم بناؤ
    ساخت طالبعلم ط;
    strcpy(ط.نام, "یحییٰ");
    ط.عمر = 21;
    ط.نمبر = 9.5;

    چھاپو("=== طالب علم کی معلومات ===\n");
    چھاپو("نام: %s\n", ط.نام);
    چھاپو("عمر: %d\n", ط.عمر);
    چھاپو("نمبر: %.1f\n", ط.نمبر);

    // نقاط بناؤ
    ساخت نقطہ ا = {0, 0};
    ساخت نقطہ ب = {3, 4};

    چھاپو("\n=== نقاط ===\n");
    چھاپو("نقطہ ا: (%d, %d)\n", ا.ایکس, ا.وائی);
    چھاپو("نقطہ ب: (%d, %d)\n", ب.ایکس, ب.وائی);
    چھاپو("فاصلہ مربع: %.0f\n", فاصلہ(ا, ب));

    واپس 0;
}
